/*
* 初始化state
 */

const initState = {
    home:'',
    market:'',
    discover:'',
    mine:''
};
/*
* reducer
 */
export default function reducer(state = initState, action) {
    switch (action.type) {
        default:
            return state
    }
}